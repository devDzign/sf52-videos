{% extends 'base.html.twig' %}

{% block title %}Edit Grade{% endblock %}

{% block body %}
    <div class="container">
        <div class="row d-flex justify-content-center align-items-center">
            <div class="col-12">
                <h1>Edit Grade</h1>

                {{ include('grade/_form.html.twig', {'button_label': 'Update'}) }}

                <div class="d-flex flex-row">
                    <a href="{{ path('grade_index') }}" class="btn btn-outline-primary mr-3">back to list</a>

                    {{ include('grade/_delete_form.html.twig') }}
                </div>

            </div>
        </div>
    </div>
{% endblock %}
