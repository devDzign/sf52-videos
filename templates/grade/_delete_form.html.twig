<form method="post" action="{{ path('grade_delete', {'id': grade.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');" class="ml-2">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ grade.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
