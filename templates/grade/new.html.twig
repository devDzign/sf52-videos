{% extends 'base.html.twig' %}

{% block title %}New Grade{% endblock %}

{% block body %}
    <div class="container">
    <div class="row d-flex justify-content-center align-items-center">
    <div class="col-12">
        <h1>Create new Grade</h1>

        {{ include('grade/_form.html.twig') }}

        <a href="{{ path('grade_index') }}" class="btn btn-dark">back to list</a>
    </div>
    </div>
    </div>

{% endblock %}
